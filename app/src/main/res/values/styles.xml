<resources>
    <!-- For the Hamburger Icon -->
    <style name="DrawerArrowStyle" parent="@style/Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@color/textColorPrimary</item>
    </style>

    <!-- Toolbar -->
    <style name="AppTheme.Toolbar" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:popupTheme">@style/AppTheme.PopupOverlay</item>
        <item name="android:theme">@style/AppTheme.AppBarOverlay</item>
    </style>

    <!-- AppBar -->
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <!-- PopupMenu -->
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat">
    </style>

    <!-- Dark Cards -->
    <style name="MinervaDarkCard" parent="CardView.Dark">
        <item name="android:layout_marginStart">@dimen/card_margin_left</item>
        <item name="android:layout_marginTop">@dimen/card_margin_top</item>
        <item name="android:layout_marginEnd">@dimen/card_margin_right</item>
        <item name="android:layout_marginBottom">@dimen/card_margin_bottom</item>
        <item name="android:foreground">?attr/selectableItemBackground</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="cardBackgroundColor">@color/card_bg_color</item>
        <item name="cardElevation">@dimen/card_elevation</item>
    </style>

    <style name="BookInfoLabelText" parent="TextAppearance.AppCompat.Button">
        <item name="android:textSize">10sp</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:layout_marginBottom">2dp</item>
    </style>

    <style name="BookInfoText" parent="TextAppearance.AppCompat.Medium">
        <item name="android:textSize">16sp</item>
    </style>

    <style name="BookInfoTextSmall" parent="TextAppearance.AppCompat.Medium">
        <item name="android:textSize">14sp</item>
    </style>
</resources>
